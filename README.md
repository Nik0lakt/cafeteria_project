
# 🍴 Cafeteria Management System & IronNet Bot

Интеллектуальная экосистема для управления корпоративной столовой. Проект объединяет биометрическую идентификацию (Face ID), автоматизацию через Telegram и мощную панель управления.

## ✨ Основные возможности (Features)

* **🤖 Telegram Бот :** Личный кабинет сотрудника. Позволяет проверять баланс, узнавать остаток лимита на день и получать уведомления о транзакциях.
* **👤 Биометрия (Face ID):** Система распознавания лиц на базе нейросетей для мгновенной идентификации сотрудников на кассе.
* **📅 Умный Календарь:** Гибкая настройка рабочих и выходных смен, поддержка пресетов (например, 2/2 или 5/2) и глобальное управление графиком.
* **🛡 Защищенная Админ-панель:** Полный контроль над штатом, ролями и финансовыми лимитами с многоуровневой защитой доступа.

---

## 🚀 Подробная установка (Installation Guide)

### 1. Подготовка окружения

Убедитесь, что на вашем сервере установлены:

* **Git** (`apt install git`)
* **Docker**
* **Docker Compose**

### 2. Клонирование репозитория

```bash
git clone https://github.com/Nik0lakt/cafeteria_project.git
cd cafeteria_project

```

### 3. Настройка конфигурации (.env)

Это самый важный шаг. Файл `.env` хранит все секретные ключи и не передается в публичный доступ.

1. Создайте файл на основе примера:
```bash
cp .env.example .env

```


2. Откройте его для редактирования:
```bash
nano .env

```


3. **Заполните следующие поля:**
* `TELEGRAM_TOKEN`: Токен вашего бота от @BotFather.
* `ADMIN_PASSWORD`: Ваш секретный пароль для входа в админку.
* `ADMIN_CHAT_ID`: Ваш Telegram ID (узнать у @userinfobot) для получения критических уведомлений.
* `DATABASE_URL`: Оставьте `sqlite:///./test.db` для стандартного запуска.



### 4. Сборка и запуск

Запустите систему одной командой. Docker сам установит зависимости Python, настроит базу данных и поднимет веб-сервер:

```bash
docker-compose up -d --build

```

### 5. Первый вход

* **Админ-панель:** `http://ваш_ip/login.html` (введите пароль из `.env`).
* **Telegram бот:** Найдите вашего бота в поиске и нажмите `/start`.

---

## 📂 Структура проекта

```text
├── app/
│   ├── main.py          # Точка входа FastAPI
│   ├── models.py        # Описание таблиц базы данных (SQLAlchemy)
│   ├── cv_utils.py      # Обработка Face ID и эмбеддингов
│   └── routers/
│       ├── auth.py      # Ядро: админка, календарь, сотрудники
│       └── bot.py       # Логика Telegram-бота IronNet
├── static/
│   ├── login.html       # Шлюз авторизации
│   ├── admin.html       # Главная панель управления
│   └── photos/          # Хранилище биометрических данных
├── .env                 # Конфиденциальные данные
└── docker-compose.yml   # Сценарий развертывания

```

---

## 👨‍🔧 Команды обслуживания

**Посмотреть, что происходит внутри (логи):**

```bash
docker logs -f cafeteria_project-web-1

```

**Перезагрузить систему после правок кода:**

```bash
docker-compose restart web

```

**Сбросить базу данных (ОПАСНО):**

```bash
rm test.db && docker-compose restart web

```

---

## 🔒 Безопасность

Все запросы к API защищены проверкой пароля на стороне сервера. Секретные ключи не «зашиты» в код, а подгружаются динамически, что соответствует стандартам промышленной разработки.

---

