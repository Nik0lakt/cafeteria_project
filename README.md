

```markdown
# 🥗 Smart Cafeteria System

**Smart Cafeteria System** — это высокотехнологичное Enterprise-решение для автоматизации корпоративного питания. Система объединяет в себе биометрическую идентификацию, умное управление финансовыми лимитами и интеграцию с производственным календарем предприятия.

---

## 📂 Структура проекта

Ниже представлена архитектура файлов и папок проекта:

```text
cafeteria_project/
├── app/                        # Бэкенд приложения (FastAPI)
│   ├── main.py                 # Точка входа и запуск бота
│   ├── database.py             # Настройка подключения к БД
│   ├── models.py               # ORM модели (Сотрудники, Транзакции, Роли)
│   ├── cv_utils.py             # Утилиты для компьютерного зрения
│   └── routers/                # Модули API
│       ├── auth.py             # Управление персоналом и графиками
│       ├── payment.py          # Логика оплаты и проверки баланса
│       ├── liveness.py         # Проверка "живости" лица
│       └── bot.py              # Telegram бот (Polling)
├── static/                     # Фронтенд и статические файлы
│   ├── index.html              # Терминал оплаты (Касса)
│   ├── admin.html              # Панель администратора
│   ├── canteen.html            # Выбор блюд
│   └── photos/                 # База биометрических снимков
├── docker-compose.yml          # Оркестрация контейнеров
├── Dockerfile                  # Инструкция сборки образа
├── requirements.txt            # Зависимости Python
├── .env.example                # Пример переменных окружения
└── .gitignore                  # Исключения для Git

```

---

## 🚀 Ключевые возможности

### 🛡 Безопасность и идентификация

* **Биометрия (Liveness Detection):** Распознавание лиц с защитой от использования фотографий или видеозаписей.
* **Аудит и отчетность:** Каждое ручное подтверждение личности сопровождается фотофиксацией и отправкой отчета в Telegram-чат администраторов.

### 💰 Финансовый контроль

* **Динамические дотации:** Гибкая настройка субсидий для различных ролей (рабочие, администрация, стажеры).
* **Защита от овердрафта:** Техническая невозможность ухода баланса в отрицательное значение за счет проверки лимита перед транзакцией.
* **Умная корзина:** Автоматическая группировка идентичных товаров в чеке для удобства кассира и покупателя.

### 📅 Управление персоналом

* **Учет сменности:** Интеграция с рабочим календарем (графики 5/2, 2/2) — дотация начисляется только в рабочие дни.
* **Telegram Бот:** Личный кабинет сотрудника в кармане. Команды `/my` и `баланс` для контроля лимитов 24/7.

---

## 🛠 Технологический стек

* **Backend:** Python 3.10+, FastAPI.
* **Database:** SQLite + SQLAlchemy ORM.
* **Frontend:** Vanilla JS, HTML5, CSS3.
* **Containerization:** Docker, Docker Compose.

---

## 📦 Развертывание

1. **Настройка окружения:** Создайте файл `.env` на основе `.example` и укажите ваш `BOT_TOKEN`.
2. **Запуск:**
```bash
docker-compose up -d --build

```


3. **Обновление:** Все изменения синхронизируются через Git и Docker volumes.

---

## 🗺 Дорожная карта (Roadmap)

В ближайших обновлениях планируется внедрение следующих функций:

* [ ] **Интеграция RFID системы:** Подключение аппаратных сканеров карт (например, [AMP-B058]) для моментальной авторизации без ручного ввода ID.
* [ ] **Аналитическая панель:** Дашборд с графиками потребления продуктов для оптимизации закупок.
* [ ] **Интеграция с 1С:** Автоматическая выгрузка отчетов о транзакциях в бухгалтерские системы.



```
