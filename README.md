# 🥗 Cafeteria Project: Система автоматизированной оплаты по NFC и Face ID

Комплексное программно-аппаратное решение для организации питания сотрудников, объединяющее бесконтактную идентификацию, биометрическую верификацию и гибкую систему управления корпоративными субсидиями.

---

## 📖 Обзор системы

Проект разработан для автоматизации корпоративных столовых. Основная цель — исключить ошибки кассира и предотвратить нецелевое использование дотаций. Система объединяет физическое оборудование (NFC-считыватели на базе Arduino) с современным веб-интерфейсом и мощным бэкендом на FastAPI.

## 🚀 Ключевые функции

### 1. Идентификация и безопасность

* **Web Serial NFC**: Прямое чтение UID карты из браузера через Web Serial API. Не требует установки драйверов или промежуточного ПО на стороне клиента.
* **Face ID Liveness Check**: Проверка "живости" лица. Система анализирует видеопоток в реальном времени, сравнивая его с эталонным фото в базе, чтобы исключить использование распечатанных фотографий.
* **Ручной режим**: Возможность ввода ID карты вручную и визуальной сверки личности кассиром в случае возникновения технических сложностей.

### 2. Финансовая логика и субсидии

* **Дневная дотация**: Накопительная система субсидий (например,  ₽ в день). Если сумма обеда превышает остаток дотации, разница списывается из личного лимита сотрудника.
* **Месячный лимит**: Учет персонального кредитного лимита (вычет из зарплаты), который обновляется в реальном времени при каждой транзакции.
* **Ролевая модель**: Динамическое управление размером дотаций в зависимости от должности сотрудника (Рабочий, ИТР и т.д.) через админ-панель.

### 3. Информирование и логирование

* **Умные чеки**: Отправка подробных квитанций в Telegram с агрегацией товаров (например, "Кофе (2 шт.) — 160 руб.") и отображением остатка средств.
* **Админ-мониторинг**: Специальный чат для администраторов, куда приходят уведомления о каждой ручной оплате с приложением двух фотографий (из базы данных и текущий кадр с камеры) для аудита.
* **Личный кабинет в Telegram**: Бот, позволяющий сотруднику мгновенно проверить свой баланс и остаток дневной дотации командой `/my`.

---

## 🛠 Технологический стек

| Слой | Технология |
| --- | --- |
| **Бэкенд** | Python 3.10, FastAPI, SQLAlchemy |
| **База данных** | PostgreSQL |
| **Фронтенд** | Vanilla JS, Web Serial API, WebRTC |
| **Биометрия** | OpenCV, DeepFace (или аналоги для эмбеддингов) |
| **Оборудование** | Arduino Uno, PN532 NFC Module |
| **DevOps** | Docker, Docker Compose |

---

## 🔌 Аппаратная часть (Hardware)

### Схема подключения:

* **Контроллер**: Arduino Uno.
* **NFC Модуль**: PN532 подключен по протоколу **I2C**.
* **Логика**: Ардуино считывает UID карты и отправляет его в Serial-порт в формате HEX-строки с символом переноса строки `\n` в конце.

---

## 🗄 Структура базы данных

1. **employees**: Хранит ФИО, роль, Telegram ID, текущий баланс и эмбеддинг лица.
2. **cards**: Связывает физический UID карты с ID сотрудника.
3. **transactions**: История всех платежей с детализацией: общая сумма, часть оплаченная дотацией, часть списанная с лимита.
4. **role_settings**: Таблица настроек дотаций для каждой должности.
5. **liveness_sessions**: Временные записи для синхронизации фронтенда и бэкенда во время проверки лица.

---

## 📦 Развертывание

1. **Настройка окружения**: Создайте файл `.env` с параметрами подключения к БД и токеном Telegram-бота.
2. **Запуск через Docker**:
```bash
docker compose up -d --build

```


3. **Использование**: При первом открытии страницы оплаты нажмите **"Подключить сканер"**, чтобы браузер получил доступ к COM-порту Arduino.

---

## 📝 Разработчику

Система использует **Live-sync** через Docker Volumes, что позволяет вносить изменения в код `app/` и `static/` без полной пересборки контейнеров. Для корректного расчета дотаций бэкенд использует строгое сравнение времени с начала текущих суток (), что предотвращает повторную выдачу субсидий в течение одного дня.
